import React from "react";

const MonitoringTitle = ({ title }) => (
  <h2 className="text-xl font-bold text-[var(--color-navy)] dark:text-white">
    {title}
  </h2>
);

export default MonitoringTitle;
